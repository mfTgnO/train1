<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserTestDataMapper">
    <resultMap id="UserResultMap" type="com.example.demo.domain.UserTestData">
        <id column="id" property="id"/>
        <result column="c_user_id" property="userId" javaType="java.lang.String"/>
        <result column="c_name" property="name" javaType="java.lang.String"/>
        <result column="c_province_id" property="provinceId" javaType="java.lang.Integer"/>
        <result column="c_city_id" property="cityId" javaType="java.lang.Integer"/>
        <result column="create_time" property="createTime" javaType="java.time.LocalDateTime"/>
    </resultMap>
    <!--<select id="selectAll" resultType="org.yyama.domain.Fruit">
        SELECT name FROM fruits
    </select>-->

    <select id="findAllUsersV1" resultMap="UserResultMap">
        select *
        from t_user LIMIT 0,100
    </select>

    <select id="findAllUsersV2" resultMap="UserResultMap">
        select *
        from t_user
    </select>

    <!--<select id="findUserById" resultMap="UserResultMap">
        select id, name, email
        from users
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.example.demo.domain.User" useGeneratedKeys="true" keyProperty="id">
        insert into users (name,email) values (#{name},#{email})
    </insert>-->


</mapper>